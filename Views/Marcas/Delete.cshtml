@model LojaSystem.Models.Marca

@{
    ViewData["Title"] = "Delete";
}


<h2>Excluir Marca</h2>
<div class="card">
    <div class="card-body">
        <h4>Certeza de que quer Excluir essa Marca?</h4>
        <div>
            <hr />
            <dl class="row">
                <dt class = "col-sm-2">
                    @Html.DisplayNameFor(model => model.NomeMarca)
                </dt>
                <dd class = "col-sm-10">
                    @Html.DisplayFor(model => model.NomeMarca)
                </dd>
            </dl>
            <br />
            <form asp-action="Delete">
                <input type="hidden" asp-for="IdMarca" />
                <input type="submit" value="Delete" class="btn btn-danger" /> |
                <a asp-action="Index">Retornar para a lista</a>
            </form>
        </div>
    </div>
</div>
